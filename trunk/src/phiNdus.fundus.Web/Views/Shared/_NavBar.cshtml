@inherits System.Web.Mvc.WebViewPage<dynamic>

<div class="navbar-inner">
    <div class="container">
        @Html.ActionLink("fundus", "Index", ControllerNames.Home, new { }, new { @class = "brand" })
                    
                    
<ul class="nav">
    <li>
        @Html.ActionLink("Shop", "Index", ControllerNames.Shop)
    </li>
    @if (Request.IsAuthenticated && User.IsInRole("User"))
    {
        <li class="dropdown">
            @if (User.IsInRole("Admin"))
            {
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Bestellungen<b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Meine Bestellungen", "Index", ControllerNames.Orders)</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Provisorische", "Pending", ControllerNames.Orders)</li>
                    <li>@Html.ActionLink("Bestätigte", "Approved", ControllerNames.Orders)</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Abgeschlossene", "Closed", ControllerNames.Orders)</li>
                    <li>@Html.ActionLink("Abgelehnte", "Rejected", ControllerNames.Orders)</li>
                </ul>
            }
            else
            {
                @Html.ActionLink("Bestellungen", "Index", ControllerNames.Orders)
            }
        </li>
        
        if (User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Verträge<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Meine Verträge", "Index", ControllerNames.Contracts)</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Unterschriebene", "Signed", ControllerNames.Contracts)</li>
                <li>@Html.ActionLink("Abgeschlossene", "Closed", ControllerNames.Contracts)</li>
            </ul>
        </li>
        }
        else
        {
        <li>
            @Html.ActionLink("Verträge", "Index", ControllerNames.Contracts)
        </li>
        }


        if (Request.IsAuthenticated && User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Material<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Neu", "Create", "Article")</li>
                <li>@Html.ActionLink("Suchen", "Index", "Article")</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Eigenschaften", "Index", ControllerNames.Fields)</li>
            </ul>
        </li>
        }

        if (User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Administration<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Benutzer", "Index", ControllerNames.Users)</li>
                <li>@Html.ActionLink("Einstellungen", "Index", ControllerNames.Settings)</li>
            </ul>
        </li>
        }
    }
</ul>

@Html.Partial("_UpperMenu")
                </div>
            </div>
        