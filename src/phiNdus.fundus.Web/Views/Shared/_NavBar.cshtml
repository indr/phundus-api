@inherits System.Web.Mvc.WebViewPage<dynamic>

<div class="navbar-inner">
    <div class="container">
        @Html.ActionLink("fundus", HomeActionNames.Index, ControllerNames.Home, new { }, new { @class = "brand" })
                    
                    
<ul class="nav">
    <li>
        @Html.ActionLink("Shop", ShopActionNames.Index, ControllerNames.Shop)
    </li>
    @if (Request.IsAuthenticated && User.IsInRole("User"))
    {
        <li class="dropdown">
            @if (User.IsInRole("Admin"))
            {
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Bestellungen<b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Meine Bestellungen", OrdersActionNames.Index, ControllerNames.Orders)</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Provisorische", OrdersActionNames.Pending, ControllerNames.Orders)</li>
                    <li>@Html.ActionLink("Bestätigte", OrdersActionNames.Approved, ControllerNames.Orders)</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Abgeschlossene", OrdersActionNames.Closed, ControllerNames.Orders)</li>
                    <li>@Html.ActionLink("Abgelehnte", OrdersActionNames.Rejected, ControllerNames.Orders)</li>
                </ul>
            }
            else
            {
                @Html.ActionLink("Bestellungen", OrdersActionNames.Index, ControllerNames.Orders)
            }
        </li>
        
        if (User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Verträge<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Meine Verträge", ContractsActionNames.Index, ControllerNames.Contracts)</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Unterschriebene",  ContractsActionNames.Signed, ControllerNames.Contracts)</li>
                <li>@Html.ActionLink("Abgeschlossene",  ContractsActionNames.Closed, ControllerNames.Contracts)</li>
            </ul>
        </li>
        }
        else
        {
        <li>
            @Html.ActionLink("Verträge",  ContractsActionNames.Index, ControllerNames.Contracts)
        </li>
        }


        if (Request.IsAuthenticated && User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Material<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Neu", ArticlesActionNames.Create, ControllerNames.Articles)</li>
                <li>@Html.ActionLink("Suchen", ArticlesActionNames.Index, ControllerNames.Articles)</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Eigenschaften", FieldsActionNames.Index, ControllerNames.Fields)</li>
            </ul>
        </li>
        }

        if (User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Administration<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Benutzer", UsersActionNames.Index, ControllerNames.Users)</li>
                <li>@Html.ActionLink("Einstellungen", SettingsActionNames.Index, ControllerNames.Settings)</li>
            </ul>
        </li>
        }
    }
</ul>

@Html.Partial("_UpperMenu")
                </div>
            </div>
        