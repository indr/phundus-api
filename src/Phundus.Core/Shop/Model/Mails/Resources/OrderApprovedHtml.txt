<p>Hallo @Model.Borrower.DisplayName</p>

<p>Die Bestellung vom @Model.Order.CreatedAtLocal mit der Nummer @Model.Order.Id wurde <b>genehmigt</b>.</p>

<p>Bitte vereinbare mit der Materialstelle telefonisch einen Übergabetermin. Wir bitten dich bei der Materialübernahme den Mietpreis bar zu bezahlen.</p>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Artikel</th>
            <th>Von</th>
            <th>Bis</th>
            <th>Menge</th>
            <th>Stückpreis</th>
            <th>Total</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var each in Model.Order.Items)
        {
            <tr>
                <td>@each.Text</td>
                <td>@each.FromLocal.ToString("d")</td>
                <td>@each.ToLocal.ToString("d")</td>
                <td>@each.Amount</td>
                <td style="text-align:right">@each.UnitPrice.ToString("N")</td>
                <td style="text-align:right">@each.LineTotal.ToString("N")</td>
            </tr>
        }
        
        <tr>
            <td colspan="5" style="text-align:right; font-weight:bold">Total</td>
            <td style="text-align:right; font-weight:bold">@Model.Order.TotalPrice.ToString("N")</td>
        </tr>
    </tbody>
</table>
