@inherits System.Web.Mvc.WebViewPage<dynamic>

<div class="navbar-inner">
    <div class="container">
        @Html.ActionLink("fundus", "Index", "Home", new { }, new { @class = "brand" })
                    
                    
<ul class="nav">
    <li>
        @Html.ActionLink("Shop", "Index", "Shop")
    </li>
    @if (Request.IsAuthenticated && User.IsInRole("User"))
    {
        <li class="dropdown">
            @if (User.IsInRole("Admin"))
            {
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Bestellungen<b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Meine Bestellungen", "Index", "Order")</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Provisorische", "Pending", "Order")</li>
                    <li>@Html.ActionLink("Bestätigte", "Approved", "Order")</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Abgeschlossene", "Closed", "Order")</li>
                    <li>@Html.ActionLink("Abgelehnte", "Rejected", "Order")</li>
                </ul>
            }
            else
            {
                @Html.ActionLink("Bestellungen", "Index", "Order")
            }
        </li>
        
        if (User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Verträge<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Meine Verträge", "Index", "Contract")</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Unterschriebene", "Signed", "Contract")</li>
                <li>@Html.ActionLink("Abgeschlossene", "Closed", "Contract")</li>
            </ul>
        </li>
        }
        else
        {
        <li>
            @Html.ActionLink("Verträge", "Index", "Contract")
        </li>
        }


        if (Request.IsAuthenticated && User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Material<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Neu", "Create", "Article")</li>
                <li>@Html.ActionLink("Suchen", "Index", "Article")</li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Eigenschaften", "Index", "Fields")</li>
            </ul>
        </li>
        }

        if (User.IsInRole("Admin"))
        {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Administration<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Benutzer", "Index", "User")</li>
                <li>@Html.ActionLink("Einstellungen", "Index", "Settings")</li>
            </ul>
        </li>
        }
    }
</ul>

@Html.Partial("_UpperMenu")
                </div>
            </div>
        